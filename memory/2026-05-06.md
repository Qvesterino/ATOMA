# 2026-05-06

- Colony semantic backbone + visual polish pass landed.
- `SafeColonyExpansion2.js`
  - colonies now produce environment lifecycle events on the shared semantic bus:
    - `environment.colony.birth`
    - `environment.colony.growth`
    - `environment.colony.merge.start`
    - `environment.colony.merge.complete`
    - `environment.colony.split.start`
    - `environment.colony.split.complete`
    - `environment.colony.transform`
    - `environment.colony.rebirth`
    - `environment.colony.mood.changed`
    - `environment.colony.pressure.active`
  - colony semantic state is now cached per colony so mood/tier/pressure emits are transition-driven instead of tick spam
  - colony truth also mirrors bounded canonical `global.*.mid/high` tags from resolved colony mood/stage/energy state
- `ColonyVFXManager.js`
  - added a new `field-membrane` colony atmosphere accent between the outer atmosphere ring and the inner core/canopy stack
  - membrane is low-cost, pooled, and wired into colony LOD / fade / split / merge / world-event paths
- `EnvironmentDomainController.js`
  - colony subsystem init now forwards `semanticBus` into the colony world-systems payload so the colony system stays controller-owned while producing semantic environment events

- Ambient entity conductor + living fauna pass landed.
  - `_AmbientEntityManager.js`
    - fixed the broken ambient behavior path:
      - entity behavior context is now stored/read under one stable `userData.behaviorContext` shape
      - behavior discriminator is now consistently `type`
      - link-flow reset no longer calls `copy()` on plain entity position objects
      - ambient link resolution now accepts live link endpoint variants (`nodeA/nodeB`, `sourceNode/targetNode`, `source/target`, `from/to`)
    - ambient manager now consumes only colony + canonical global semantic tags through `EventRegistrationRegistry`
    - controller/passive ecosystem baseline remains active, but short-lived colony/global semantic windows now bias spawn mix, motion, and local emergence
    - added bounded colony-driven semantic spawn pulses for birth/growth/merge/split/transform/rebirth/pressure beats
    - ghost orb / spectre / swarm / wisp visuals were retuned toward a cleaner “living fauna” read without adding a new heavy shader family
  - `EnvironmentDomainController.js`
    - ambient subsystem registration now forwards `semanticBus`
    - ambient subsystem is re-registered after colony bootstrap so colony-driven ambient behavior is not initialized with a null colony anchor
    - ambient now receives controller-owned world context / `releaseAtmosphere` through the same sync path as `SafeWorldFXPack` and `EnvironmentalHazards`
  - `main.js`
    - fallback ambient setup now uses the same object-style registration contract and forwards `semanticBus`
