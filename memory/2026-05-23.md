# 2026-05-23

## Run Identity Overlay Friction Fix
- Implemented a UX-only run-identity persistence split so the selector no longer auto-opens on every start.
- Added `menuProfile.runIdentityUX` with:
  - `version = 1`
  - `seenOverlayByWorld.quantum`
  - `seenOverlayByWorld.desert`
- `RunIdentityDirector` now distinguishes:
  - `auto` entry mode
  - `manual` entry mode
- Auto mode now shows the run-identity selector only once per world for `quantum` and `desert`.
- Confirming the selector marks the current world as seen; later starts use the last selected package/world-state automatically with no extra click.
- Manual access was added in:
  - `MainMenu`
  - `PauseMenu`
- Manual open reuses the existing `RunIdentityDirector` overlay and adds a non-destructive `Keep Current` close path.
- Runtime orchestration detail:
  - auto overlay still pauses/resumes on first world entry
  - manual overlay applies the selection immediately but does not auto-resume from pause
- Smoke result on `http://127.0.0.1:5173/`:
  - first Quantum start shows overlay once
  - later Quantum starts skip it
  - first Desert start shows overlay once
  - later Desert starts will use the stored selection
  - manual reopen works from Main Menu and Pause Menu
- Known unrelated console noise still present:
  - `[MetricAuthorityGuard] Invalid writer metrics for: HarmonicHealingRecoveryVisualSystem`
