# 2026-05-10

- Prepared GitHub Pages deployment path for the Vite build.
- `vite.config.js` now resolves `base` from `ATOMA_BASE_PATH` or GitHub Actions repository metadata instead of the stale local `/ATOMA_CLEAN/` path.
- Added `.github/workflows/deploy-pages.yml` for build-and-deploy through GitHub Pages Actions on the default branch.
- Release-readiness fix: `HarmonicResonanceFeedbackSystem.js` particle attributes now use valid `THREE.BufferAttribute` + `Float32Array` construction.
- Build verification passed for a Pages-style base path `/ATOMA_OFFICIAL/`.
- Remaining build warnings are currently limited to `Tone.__ATOMA_TONE_STUB__` export detection and large bundle size.
