## 2026-05-14

### Harmonic/Healing/Harmony Systems Audit & Cleanup — FÁZA 1

**Cieľ**: Presunúť neaktívne/redundantné systémy do LEGACY a odpojiť ich z runtime.

#### Prečítaných 17 súborov (~11 500 riadkov)
- 12 core harmonic/healing/harmony systémov
- 5 podporných súborov (shadery, utility, particles)

#### Identifikované problémy
1. **Duplicitné vizuály**: `HarmonicHubAuraSystem` + `HarmonicNodeResonanceHalos` — oba robia halo/aura okolo hubov
2. **Neaktívny kód**: `HarmonicInfluencePropagationSystem` má `MOTION_OFF_PHASE1 = true` (vypnutý core motion)
3. **Duplicitné helper patterny**: `clamp01`, `readNodeHarmony`, `getLinkEndpoints` sa opakujú v ~8 súboroch
4. **Merged súbory**: `HarmonicHubCascade.js`, `HarmonicHubLifecycle.js`, `HarmonicHubSync.js` — každý obsahuje 3 triedy

#### FÁZA 1 — Dokončené (2026-05-14)

**Presunuté do LEGACY:**
- `HarmonicInfluencePropagationSystem_Session127.js` → `LEGACY/HarmonicInfluencePropagationSystem_Session127.js`
- `HarmonicNodeResonanceHalos.js` → `LEGACY/HarmonicNodeResonanceHalos.js`

**Dôvody:**
- `HarmonicInfluencePropagationSystem`: `MOTION_OFF_PHASE1 = true` — core motion vypnutý, redundantný s `HarmonicHubAuraSystem`
- `HarmonicNodeResonanceHalos`: Jednoduché disk halos redundantné s komplexnými field meshes z `HarmonicHubAuraSystem`

**Odpojené referencie v:**
- `main.js` — import, property, setup method, tick method, frameScheduler registration, regGuard, late-wire v `setupInfluenceReflection`
- `Engine/Debug/FXDebugSandbox.js` — import, 6x debug panel entries, spawn method
- `InfluenceReflectionBackPressureSystem_Session129.js` — docstring update
- `StandingWaveOscillationTrapSystem_Session130.js` — docstring update
- `ai tools (powershell)/vfx_contract_audit.js` — removed from contract list

**Bezpečnostné opatrenia:**
- `InfluenceReflectionBackPressureSystem` dostáva `null` namiesto `harmonicInfluenceSystem` — má graceful fallback (line 322: `if (this.harmonicInfluenceSystem && ...)`)
- Všetky odstránené časti sú označené komentárom `// REMOVED: ... — moved to LEGACY (2026-05-14)`

#### FÁZA 2 — Dokončené (2026-05-14)

**Vytvorené `shared/harmonyHelpers.js`**
- 23 centralizovaných pure utility funkcií (zero dependencies, zero allocations)
- Math: `clamp01`, `clamp`, `lerp`, `smoothstep`, `normalizePhase`, `lerpPhase`
- Hash: `hashString`, `hashToUnit`
- Node readers: `readNodeMetric`, `readNodeHarmony`, `readNodeCorruption`, `readNodeStability`, `readNodeSynergy`, `readNodeInstability`, `readNodeLoadPressure`
- Link readers: `readLinkMetric`, `readLinkSynergy`, `readLinkHarmony`
- Hub readers: `readHubMetric`
- Geometry: `getNodePosition`, `getLinkEndpoints`, `distance3D`
- Cooldown: `checkCooldown`, `setCooldown`
- Write: `writeNodeMetric`, `writeLinkMetric`
- Resolve: `resolveNodeId`, `resolveLinkId`

**Rozdelené merged súbory do `harmonic/` foldera:**

| Pôvodný merged súbor | Nové súbory v `harmonic/` | Barrel (root) |
|---|---|---|
| `HarmonicHubCascade.js` (781 riadkov, 3 triedy) | `HubProximityDetector.js`, `HarmonicPhaseSynchronization_Session146.js`, `CascadingHarmonicResonanceAmplification.js` | `HarmonicHubCascade.js` (re-exports) |
| `HarmonicHubLifecycle.js` (884 riadkov, 3 triedy) | `HarmonicHubCollapseController.js`, `HarmonicHubRecoveryController.js`, `HarmonicHubResilienceController.js` | `HarmonicHubLifecycle.js` (re-exports) |
| `HarmonicHubSync.js` (746 riadkov, 3 triedy) | `NodeHarmonicSyncController.js`, `HarmonicSyncEffectApplier.js`, `NodeHarmonicManager.js` | `HarmonicHubSync.js` (re-exports) |

**Refaktoring v nových súboroch:**
- `HubProximityDetector.js` — používa `readHubMetric` a `clamp01` z `shared/harmonyHelpers.js`
- `HarmonicPhaseSynchronization_Session146.js` — používa `normalizePhase` z helpers
- `CascadingHarmonicResonanceAmplification.js` — používa `clamp` a `clamp01` z helpers
- `HarmonicHubCollapseController.js` — používa `normalizePhase` z helpers (odstránená duplicitná metóda)
- `NodeHarmonicSyncController.js` — používa `clamp01`, `lerpPhase`, `normalizePhase` z helpers (odstránené duplicitné metódy)

**Backward compatibility:**
- Všetky existujúce importy cez root barrel súbory (`./HarmonicHubCascade.js`, `./HarmonicHubLifecycle.js`, `./HarmonicHubSync.js`) fungujú bez zmeny
- `main.js`, `HarmonicCascadeAmplification_Session145.js`, `LinkRendererConduit.js` — žiadne zmeny potrebné

#### FÁZA 3 — Dokončené (2026-05-14)

**Merge `HarmonicHealingVisualSystem` + `HarmonicRecoveryVisualSystem`**
- Vytvorený `HarmonicHealingRecoveryVisualSystem.js` — jeden merged systém
- Staré súbory presunuté do `LEGACY/`:
  - `HarmonicHealingVisualSystem_Session134.js`
  - `HarmonicRecoveryVisualSystem_Session138.js`

**Architektúra merged systému:**
- 100% event-driven — odstránený `_resolveHealingState` guláš (čítal z `window.__ATOMA_LIVE_METRICS__`, `networkState.harmonyFlow`, atď.)
- Používa IBA canonical scoped metric tier eventy:
  - `link.harmony.high/mid/low` — traveling waves + recovery vizuály
  - `link.corruption.high` — healing wave response
  - `link.stability.low/mid` — repair + recovery
  - `node.corruption.high` — waves on connected links
  - `node.stability.low` — preparatory recovery halo
- Zachované: rupture monitoring, dramaturgy modulation, topology.healing emission
- Zachované: všetky pool-y (zero-allocation), shader materials, cooldown systém

**Zmeny v `main.js`:**
- Import: `HarmonicHealingRecoveryVisualSystem` namiesto dvoch importov
- Property: `this.harmonicHealingRecovery` namiesto `this.harmonicHealing` + `this.harmonicRecovery`
- Setup: `setupHarmonicHealingRecoverySystem()` namiesto `setupHarmonicHealingSystem()` + `setupHarmonicRecovery()`
- FrameScheduler: jeden tick `visual.harmonicHealingRecovery` namiesto dvoch
- Rebind: jeden rebind call
- Dramaturgy: jeden `setDramaturgyModulation` call
- Debug bridge: `dbg.healingRecovery` namiesto `dbg.recovery`
- RegionalEquilibrium bridge: `setRegionalPriority` na merged systém

**Zmeny v `src/metrics/MetricAuthorityGuard.js`:**
- `HarmonicHealingVisualSystem_Session134` writer odstránený (bol v LEGACY)
- Nový `HarmonicHealingRecoveryVisualSystem` writer: read-only adapter, zero metric writes

**Zmeny v `ai tools (powershell)/vfx_contract_audit.js`:**
- `HarmonicHealingVisualSystem_Session134` → odstránený (LEGACY)
- `HarmonicRecoveryVisualSystem_Session138` → odstránený (LEGACY)
- `HarmonicHealingRecoveryVisualSystem.js` → pridaný

#### Zostáva na budúce
- Presunúť súbory do `systems/harmony/`, `systems/healing/`, `systems/aura/` (folder reorganization)
- Aktualizovať dokumentáciu v `docs/audits/` ak existuje
