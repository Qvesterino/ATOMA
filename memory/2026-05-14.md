## 2026-05-14

### Harmonic/Healing/Harmony Systems Audit & Cleanup — FÁZA 1

**Cieľ**: Presunúť neaktívne/redundantné systémy do LEGACY a odpojiť ich z runtime.

#### Prečítaných 17 súborov (~11 500 riadkov)
- 12 core harmonic/healing/harmony systémov
- 5 podporných súborov (shadery, utility, particles)

#### Identifikované problémy
1. **Duplicitné vizuály**: `HarmonicHubAuraSystem` + `HarmonicNodeResonanceHalos` — oba robia halo/aura okolo hubov
2. **Neaktívny kód**: `HarmonicInfluencePropagationSystem` má `MOTION_OFF_PHASE1 = true` (vypnutý core motion)
3. **Duplicitné helper patterny**: `clamp01`, `readNodeHarmony`, `getLinkEndpoints` sa opakujú v ~8 súboroch
4. **Merged súbory**: `HarmonicHubCascade.js`, `HarmonicHubLifecycle.js`, `HarmonicHubSync.js` — každý obsahuje 3 triedy

#### FÁZA 1 — Dokončené (2026-05-14)

**Presunuté do LEGACY:**
- `HarmonicInfluencePropagationSystem_Session127.js` → `LEGACY/HarmonicInfluencePropagationSystem_Session127.js`
- `HarmonicNodeResonanceHalos.js` → `LEGACY/HarmonicNodeResonanceHalos.js`

**Dôvody:**
- `HarmonicInfluencePropagationSystem`: `MOTION_OFF_PHASE1 = true` — core motion vypnutý, redundantný s `HarmonicHubAuraSystem`
- `HarmonicNodeResonanceHalos`: Jednoduché disk halos redundantné s komplexnými field meshes z `HarmonicHubAuraSystem`

**Odpojené referencie v:**
- `main.js` — import, property, setup method, tick method, frameScheduler registration, regGuard, late-wire v `setupInfluenceReflection`
- `Engine/Debug/FXDebugSandbox.js` — import, 6x debug panel entries, spawn method
- `InfluenceReflectionBackPressureSystem_Session129.js` — docstring update
- `StandingWaveOscillationTrapSystem_Session130.js` — docstring update
- `ai tools (powershell)/vfx_contract_audit.js` — removed from contract list

**Bezpečnostné opatrenia:**
- `InfluenceReflectionBackPressureSystem` dostáva `null` namiesto `harmonicInfluenceSystem` — má graceful fallback (line 322: `if (this.harmonicInfluenceSystem && ...)`)
- Všetky odstránené časti sú označené komentárom `// REMOVED: ... — moved to LEGACY (2026-05-14)`

#### FÁZA 2 — Dokončené (2026-05-14)

**Vytvorené `shared/harmonyHelpers.js`**
- 23 centralizovaných pure utility funkcií (zero dependencies, zero allocations)
- Math: `clamp01`, `clamp`, `lerp`, `smoothstep`, `normalizePhase`, `lerpPhase`
- Hash: `hashString`, `hashToUnit`
- Node readers: `readNodeMetric`, `readNodeHarmony`, `readNodeCorruption`, `readNodeStability`, `readNodeSynergy`, `readNodeInstability`, `readNodeLoadPressure`
- Link readers: `readLinkMetric`, `readLinkSynergy`, `readLinkHarmony`
- Hub readers: `readHubMetric`
- Geometry: `getNodePosition`, `getLinkEndpoints`, `distance3D`
- Cooldown: `checkCooldown`, `setCooldown`
- Write: `writeNodeMetric`, `writeLinkMetric`
- Resolve: `resolveNodeId`, `resolveLinkId`

**Rozdelené merged súbory do `harmonic/` foldera:**

| Pôvodný merged súbor | Nové súbory v `harmonic/` | Barrel (root) |
|---|---|---|
| `HarmonicHubCascade.js` (781 riadkov, 3 triedy) | `HubProximityDetector.js`, `HarmonicPhaseSynchronization_Session146.js`, `CascadingHarmonicResonanceAmplification.js` | `HarmonicHubCascade.js` (re-exports) |
| `HarmonicHubLifecycle.js` (884 riadkov, 3 triedy) | `HarmonicHubCollapseController.js`, `HarmonicHubRecoveryController.js`, `HarmonicHubResilienceController.js` | `HarmonicHubLifecycle.js` (re-exports) |
| `HarmonicHubSync.js` (746 riadkov, 3 triedy) | `NodeHarmonicSyncController.js`, `HarmonicSyncEffectApplier.js`, `NodeHarmonicManager.js` | `HarmonicHubSync.js` (re-exports) |

**Refaktoring v nových súboroch:**
- `HubProximityDetector.js` — používa `readHubMetric` a `clamp01` z `shared/harmonyHelpers.js`
- `HarmonicPhaseSynchronization_Session146.js` — používa `normalizePhase` z helpers
- `CascadingHarmonicResonanceAmplification.js` — používa `clamp` a `clamp01` z helpers
- `HarmonicHubCollapseController.js` — používa `normalizePhase` z helpers (odstránená duplicitná metóda)
- `NodeHarmonicSyncController.js` — používa `clamp01`, `lerpPhase`, `normalizePhase` z helpers (odstránené duplicitné metódy)

**Backward compatibility:**
- Všetky existujúce importy cez root barrel súbory (`./HarmonicHubCascade.js`, `./HarmonicHubLifecycle.js`, `./HarmonicHubSync.js`) fungujú bez zmeny
- `main.js`, `HarmonicCascadeAmplification_Session145.js`, `LinkRendererConduit.js` — žiadne zmeny potrebné

#### FÁZA 3 — Dokončené (2026-05-14)

**Merge `HarmonicHealingVisualSystem` + `HarmonicRecoveryVisualSystem`**
- Vytvorený `HarmonicHealingRecoveryVisualSystem.js` — jeden merged systém
- Staré súbory presunuté do `LEGACY/`:
  - `HarmonicHealingVisualSystem_Session134.js`
  - `HarmonicRecoveryVisualSystem_Session138.js`

**Architektúra merged systému:**
- 100% event-driven — odstránený `_resolveHealingState` guláš (čítal z `window.__ATOMA_LIVE_METRICS__`, `networkState.harmonyFlow`, atď.)
- Používa IBA canonical scoped metric tier eventy:
  - `link.harmony.high/mid/low` — traveling waves + recovery vizuály
  - `link.corruption.high` — healing wave response
  - `link.stability.low/mid` — repair + recovery
  - `node.corruption.high` — waves on connected links
  - `node.stability.low` — preparatory recovery halo
- Zachované: rupture monitoring, dramaturgy modulation, topology.healing emission
- Zachované: všetky pool-y (zero-allocation), shader materials, cooldown systém

**Zmeny v `main.js`:**
- Import: `HarmonicHealingRecoveryVisualSystem` namiesto dvoch importov
- Property: `this.harmonicHealingRecovery` namiesto `this.harmonicHealing` + `this.harmonicRecovery`
- Setup: `setupHarmonicHealingRecoverySystem()` namiesto `setupHarmonicHealingSystem()` + `setupHarmonicRecovery()`
- FrameScheduler: jeden tick `visual.harmonicHealingRecovery` namiesto dvoch
- Rebind: jeden rebind call
- Dramaturgy: jeden `setDramaturgyModulation` call
- Debug bridge: `dbg.healingRecovery` namiesto `dbg.recovery`
- RegionalEquilibrium bridge: `setRegionalPriority` na merged systém

**Zmeny v `src/metrics/MetricAuthorityGuard.js`:**
- `HarmonicHealingVisualSystem_Session134` writer odstránený (bol v LEGACY)
- Nový `HarmonicHealingRecoveryVisualSystem` writer: read-only adapter, zero metric writes

**Zmeny v `ai tools (powershell)/vfx_contract_audit.js`:**
- `HarmonicHealingVisualSystem_Session134` → odstránený (LEGACY)
- `HarmonicRecoveryVisualSystem_Session138` → odstránený (LEGACY)
- `HarmonicHealingRecoveryVisualSystem.js` → pridaný

#### FÁZA 4 — Dokončené (2026-05-14)

**Audit canonical harmony tiered eventov**
- Preverených 13 súborov (10 root + 3 barrel)
- **Canonical harmony tiered eventy** (povolené): `node.harmony.low/mid/high`, `link.harmony.low/mid/high`, `hub.harmony.low/mid/high`, `global.harmony.low/mid/high`

**✅ OK systémy (používajú IBA canonical harmony tiered eventy):**
1. `HarmonicHubAuraSystem_Session126.js` — `hub.harmony.high/mid/low`
2. `HarmonicCascadeAmplification_Session145.js` — žiadne subscribe/emit
3. `HarmonicAudioReactivitySystem_Session135.js` — žiadne subscribe/emit
4. `HarmonicResonanceCoupling_v1.js` — žiadne subscribe/emit
5. `HarmonicResonanceFeedbackSystem.js` — žiadne subscribe/emit
6. `HarmonyAuraShaderMaterial.js` — žiadne subscribe/emit
7. `HarmonyStabilization.js` — žiadne subscribe/emit
8. `HealingParticleSystem_Session136.js` — žiadne subscribe/emit
9. `HarmonicHubCascade.js` (barrel) — žiadne subscribe/emit
10. `HarmonicHubLifecycle.js` (barrel) — žiadne subscribe/emit
11. `HarmonicHubSync.js` (barrel) — žiadne subscribe/emit

**⚠️ VIOLATIONS (legitímne, ponechané tak):**
1. `HarmonicHealingRecoveryVisualSystem.js` — subscribe: `link.corruption.high`, `link.stability.low/mid`, `node.corruption.high`, `node.stability.low` (bridge eventy pre healing response); emit: `topology.healing`
2. `HarmonicHubAuraSystem_Session126.js` — emit: `metric.tier.changed` (diagnostics), `harmonic.cascade.start` (cascade bridge)
3. `harmonic/CascadingHarmonicResonanceAmplification.js` — emit: `cascade.start` (cascade-specific)
4. `HarmonicTopologyLearningSystem.js` — subscribe: `topology.rupture`, `topology.healing` (topology bridge)

**Rozhodnutie:** Violations majú legitímne použitie ako bridge/diagnostic eventy. Ponechané tak.

#### FÁZA 5 — Dokončené (2026-05-14)

**Organizácia do `harmony/` priečinku**
- Vytvorený priečinok `harmony/`
- Presunutých 13 súborov:
  - `HarmonicCascadeAmplification_Session145.js`
  - `HarmonicAudioReactivitySystem_Session135.js`
  - `HarmonicHealingRecoveryVisualSystem.js`
  - `HarmonicHubAuraSystem_Session126.js`
  - `HarmonicResonanceCoupling_v1.js`
  - `HarmonicResonanceFeedbackSystem.js`
  - `HarmonicTopologyLearningSystem.js`
  - `HarmonyAuraShaderMaterial.js`
  - `HarmonyStabilization.js`
  - `HealingParticleSystem_Session136.js`
  - `HarmonicHubCascade.js` (barrel)
  - `HarmonicHubLifecycle.js` (barrel)
  - `HarmonicHubSync.js` (barrel)

**Aktualizované importy:**
- `main.js` — 12 importov aktualizovaných na `./harmony/...`
- `NodeLinkingSystem.js` — `HarmonyAuraShaderMaterial.js`
- `LinkRendererConduit.js` — `HarmonicHubSync.js`
- `harmony/HarmonicCascadeAmplification_Session145.js` — `HarmonicHubCascade.js`
- `src/vfx/VFXSystemRegistry.js` — `HarmonicResonanceFeedbackSystem.js`, `HarmonyAuraShaderMaterial.js`
- `tests/MetricsAuthority.test.js` — `HarmonicHubAuraSystem_Session126.js`
- `tests/GameplayLoopChecks.js` — `HarmonyStabilization.js`
- `ai tools (powershell)/vfx_contract_audit.js` — všetky 7 harmony súborov s prefixom `harmony/`

**Aktualizované relatívne cesty v `harmony/` súboroch:**
- 7 súborov malo nesprávne `./` cesty na shared/, Engine/, src/, HUD/, VisualHierarchyRegistry
- Všetky fixnuté na `../` prefix

**Aktualizované barrel files:**
- `harmony/HarmonicHubCascade.js` — importy z `../harmonic/...`
- `harmony/HarmonicHubLifecycle.js` — importy z `../harmonic/...`
- `harmony/HarmonicHubSync.js` — importy z `../harmonic/...`

#### FÁZA 6 — Event-driven triggers pridané (2026-05-14)

**`HealingParticleSystem_Session136.js`**
- Pridaný `setEventBus(semanticBus)` s EventRegistrationRegistry
- `node.harmony.high` → `emitSplash()` (celebration burst na node pozícii)
- `link.harmony.high` → `emitHealingTrail()` (trail pozdĺž linku)
- Pridané helper metódy `_findNodeById()`, `_findLinkById()`

**`HarmonicAudioReactivitySystem_Session135.js`**
- Pridaný `setEventBus(semanticBus)` s EventRegistrationRegistry
- `global.harmony.high/mid/low` → `updateNetworkState()` (modulácia ambient zvuku)
- `node.harmony.high` → `triggerHealingTone()` (432Hz healing tone)
- `link.corruption.high` → `triggerRuptureSound()` (dissonant rupture impact)

**Aktualizované `main.js`:**
- `this.harmonicAudio.setEventBus?.(this.semanticBus)` v `setupHarmonicHealingRecoverySystem()`
- `this.healingParticles.setEventBus?.(this.semanticBus)` v `setupHarmonicHealingRecoverySystem()`

**Ponechané tick-based (legitímne dôvody):**
- `HarmonicResonanceCoupling_v1` — per-frame shimmer/glow modulation na všetkých linkoch
- `HarmonicResonanceFeedbackSystem` — per-frame field update a phase drift
- `HarmonicCascadeAmplification_Session145` — interná cascade logika
- `HarmonyStabilization` — metric writer (nie visual consumer)
- `HarmonyAuraShaderMaterial` — iba shader material

#### FÁZA 7 — Corruption Audit & Canonical Event Triggers (2026-05-14)

**Identifikované corruption systémy (10):**
1. `CorruptionVisualFX_v1.js` — tick-based, žiadne eventy
2. `CascadingRuptureSystem.js` — emituje `topology.rupture` (non-canonical)
3. `CriticalNodeFailureSystem.js` — tick-based
4. `LinkCorruptionTransmission_v1.js` — emituje `metric:corruptionRise`, `topology.healing` (non-canonical)
5. `T2_CorruptionVisualIntegration_v1.js` — subscribe `node.corruption.high` (canonical) ale **bare `bus.on()` bez EventRegistrationRegistry**
6. `TIER4_CorruptionFeedbackVisuals_v1.js` — tick-based
7. `EventDramaturgyEngine.js` — corruption profile s `network:corruptionSpread` (non-canonical)
8. `EnvironmentalHazards.js` — subscribe `global.corruption.high` (canonical)
9. `CascadeEventBridge_v1.js` — subscribe `node.corruption.high` (canonical)
10. `HarmonyStabilization.js` — emituje všetky corruption tiered eventy (canonical, ako writer)

**Pridané canonical corruption tiered event triggers:**

**`CascadingRuptureSystem.js`**
- Pridaný `setEventBus(semanticBus)` s EventRegistrationRegistry
- `node.corruption.high` → `triggerCascade(node, 0.9)` (high energy cascade)
- `node.corruption.mid` → `triggerCascade(node, 0.6)` (mid energy cascade)
- `node.corruption.low` → `triggerCascade(node, 0.3)` (low energy cascade)
- Pridané helper metódy `_findNodeById()`

**`LinkCorruptionTransmission_v1.js`**
- Pridaný `setEventBus(semanticBus)` s EventRegistrationRegistry
- `link.corruption.high` → `_injectCorruptionPulse(link, 0.25)`
- `link.corruption.mid` → `_injectCorruptionPulse(link, 0.15)`
- `link.corruption.low` → `_injectCorruptionPulse(link, 0.05)`
- `node.corruption.high` → `_infectNodeLinks(node, 0.2)` (šíri corruption na všetky linky node)
- `node.corruption.mid` → `_infectNodeLinks(node, 0.1)`
- Pridané helper metódy `_findLinkById()`, `_findNodeById()`, `_injectCorruptionPulse()`, `_infectNodeLinks()`
- `dispose()` volá `eventRegistrationRegistry.disposeOwner('LinkCorruptionTransmission_v1')`

**`EventDramaturgyEngine.js`**
- Pridaný import `eventRegistrationRegistry`
- Corruption profile triggerEvents zmenené z `['network:corruptionSpread', 'node.corruption.high']` na `['global.corruption.high', 'global.corruption.mid', 'global.corruption.low']`
- Pridaný `setEventBus(semanticBus)` — umožňuje re-subscribe pri bus zmene
- `_busSubscribe()` preferuje `eventRegistrationRegistry.register()` pre canonical event tracking
- `dispose()` volá `eventRegistrationRegistry.disposeOwner('EventDramaturgyEngine')`

**Aktualizované `main.js`:**
- `this.cascadingRuptures.setEventBus?.(this.semanticBus)` v `setupCascadingRuptureAndFailure()`
- `this.corruptionTransmission.setEventBus?.(this.semanticBus)` v `createAINodes()`

**Ponechané bez zmien (legitímne dôvody):**
- `T2_CorruptionVisualIntegration_v1.js` — bare `bus.on()` violation, ale lokálny visual consumer
- `EnvironmentalHazards.js` — už používa canonical `global.corruption.high`
- `CascadeEventBridge_v1.js` — už používa canonical `node.corruption.high`
- `HarmonyStabilization.js` — metric writer, legitímne emituje canonical tiered eventy

#### Zostáva na budúce
- Aktualizovať dokumentáciu v `docs/audits/` ak existuje
- Zvážiť ďalšiu organizáciu podpriečinkov v `harmony/` (napr. `harmony/hub/`, `harmony/healing/`)
- Preskúmať `T2_CorruptionVisualIntegration_v1.js` — bare `bus.on()` bez EventRegistrationRegistry
