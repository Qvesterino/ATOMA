# 2026-05-04

- Confirmed the linked pale-cyan orbit package is strongly driven by `EmergentThoughtStorms5_0`.
- Live runtime on `?diag=dreamdepth-camera-overlay` showed:
  - `proximityRadius = 2.75`
  - `coreGlyphCount = 12`
  - `2` active storm clusters at once
  - `24` total glyphs
- Disabling `window.game.emergentThoughtStorms` in runtime immediately dropped:
  - `activeClusters: 2 -> 0`
  - `glyphCount: 24 -> 0`
- Default boot behavior was changed in `main.js` so `setupEmergentThoughtStorms()` now initializes the system but leaves it disabled by default.
- Console toggles remain available for A/B checks: `toggleThoughtStorms()`, `debugThoughtStorms()`.
- Restored the normal `EmergentThoughtStorms5_0` import in `main.js`; only the default enabled state stays OFF.
- Added a shared 2.0s line-ripple cooldown gate to `SynergyCascadeVisualizer` so cascade ring/line ripples cannot spam through alternate spawn paths.
- Hard-disconnected EmergentThoughtStorms5_0 from runtime: import commented, environment-domain injection commented, update hooks disabled, setup method reduced to a no-op, lore bridge disabled, storm debug commands removed, and narrative patterns no longer require/pass the storms instance.
- Current release-safe state: keep `EmergentThoughtStorms5_0` as an orphan file on disk and do not reattach it to runtime unless intentionally re-integrating it.
- Performance / FX follow-up notes:
  - `WaveParticleEmitter_v1` is currently only partially functional: synergy / helix / shard-style emissions still appear, but the rest of the expected wave particle families are effectively missing in live gameplay.
  - `Canonical Template 3 Stress Visual` is currently intentionally disabled / dead because it was contributing glitchy keyhole-like midpoint effects near world origin.
  - Desired future restoration: bring back the good large wavy plane / half-world atmospheric layer from the old stress visual, but keep the bad midpoint-origin artifact path disabled.
- TODO for future reactivation of `EmergentThoughtStorms5_0`:
  - Decide one authority only. Preferred: `EnvironmentDomainController` OR direct `main.js` ownership, not both.
  - If re-enabling via `main.js`, restore:
    - import in `main.js`
    - dependency injection entries passed into `EnvironmentDomainController` in both initial construction and world recreation paths
    - `setupEmergentThoughtStorms()`
    - visual scheduler hook / `regGuard` update path
    - optional lore bridge `_wireThoughtStormLoreBridge()`
    - optional debug commands (`toggleThoughtStorms`, `debugThoughtStorms`, etc.)
  - If re-enabling via `EnvironmentDomainController`, restore:
    - storms registry/dependency creation branch in `EnvironmentDomainController.init()`
    - `main.js` assignment from `environmentDomain.instances.emergentThoughtStorms`
    - only one update owner, do not double-update in both domain and main loop
  - `RecursiveGlyphSignalSystem.setDynamicsContext({ isBurstActive })` previously read `emergentThoughtStorms.activeStorms`; if storms come back, this hook can be restored.
  - `AINarrativePatterns6_0` currently receives `null` for the storms parameter; this is safe today because the constructor only stores the reference. If future narrative logic starts using storms directly, restore the injected instance there too.
- Performance cadence follow-up:
  - `NodeInspectOverlay1_0` was cut to effective `10Hz` both internally (`checkInterval = 0.1`) and at the `main.js` wrapper tick, instead of being called every visual frame.
  - `NodeLinkedAuraSystem` now splits heavy flame vertex deformation from cheap opacity/orbit updates:
    - opacity/orbit/transform/shader time still run on the normal visual tick
    - `applyFlameMotion()` now defaults to a slower shared cadence (`~15Hz`) via `flameMotionInterval`
- Release perf / collapse follow-up:
  - `LinkCollapseSystem` default semantics changed to release mode: `corruption high OR stability low`, with default `holdDurationMs = 5000`.
  - `LinkCollapseSystem` remains event-driven; no per-frame collapse scan was reintroduced.
  - `LinkPulsePhaseSync` now has hub-only lazy gating in the directional streak path, so ordinary non-hub links skip the phase-sync update/apply work.
  - `PersonalityShaderBridge_v1` stayed enabled, but now skips dormant zero-signal passes and reports richer runtime stats through `window.__ATOMA_PERSONALITY_SHADER_REPORT__()`.
  - `personalityShaderEffects` is now explicitly marked runtime-dormant (`enabled = false`, `runtimeStatus = 'dormant-unwired'`) until it is actually wired to material registration in production.
- Release gameplay loop lock-in:
  - `VisualNetworkTimeElasticity_v1` remains the single gameplay-score authority, but release defaults are now unified at `forwardSpeed = 5`, `rewindSpeed = 3.5`, `sustainDuration = 5.0`, canonical `global.synergy.high`.
  - World-specific score drift was removed from both `main.js` and `MainMenu.js`; all active worlds now mirror the same release score rule.
  - Existing extra layers stay active: pressure escalation, combo stacking, drama zone.
  - Added run-level mastery stats to the score authority: `rewindActivations`, `totalHighSynergyTime`, `averageRewindSynergy`, `rewindUptimeRatio`.
  - `AtomaLeaderboard` scoring was reweighted toward synergy mastery and rewind quality/uptime, not just raw completion time.
  - `tests/GameplayLoopChecks.js` now covers: 5s sustain activation, sustain reset, exact win-to-zero, combo/drama/escalation preservation, unified world config, and mastery-biased leaderboard ordering.
