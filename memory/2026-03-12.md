# 2026-03-12

- NodeLinkingSystem box-select updated for in-game pointer-lock flow.
- New behavior: `LMB hold >= 200ms + drag` starts box selection from crosshair (screen center), using relative mouse deltas (`movementX/Y`).
- Existing non-pointer-lock behavior preserved (empty-space drag).
- Box selection: camera mouse-look is now paused during active box drag and restored on release/ESC.
- Corruption/wave pipeline activation pass:
  - `NodeLinkingSystem.getLinkMetricsSnapshot()` expanded to read canonical link metrics (`userData.metrics`, `userData.*`) so Conduit VFX receives live harmony/corruption/synergy/stability values.
  - `HarmonicInfluencePropagationSystem_Session127` fixed runtime blocker (`Math.clamp` -> safe clamp) and link/node compatibility (`source/target` + `userData.nodeId`).
  - `InfluenceReflectionBackPressureSystem_Session129` patched for canonical node/link shape reads and canonical node metrics fallback.
  - `StandingWaveOscillationTrapSystem_Session130` critical bug fixed: `reflectionHistory` now `Map` (was array but used as Map), plus canonical link/node/metrics compatibility.
  - `ResonanceRuptureVisualSystem_Session133` patched for canonical link/node/metrics compatibility.
  - `main.js:setupHarmonicInfluencePropagation()` now passes world with `linkingSystem` and late-wires influence system into reflection/trap systems initialized earlier.
- TASK2 (LinkCorruptionSpreadAnimator reliability):
  - Removed global animator call in `LinkRendererConduit.updateAll()` (wrong signature, no-op).
  - Kept only per-link call path and now passes canonical `metrics.corruption`.
  - `LinkCorruptionSpreadAnimator` now reads corruption from canonical sources (`options.corruptionLevel`, `link.userData.metrics.corruption`, fallbacks).
  - Added trigger debounce/cooldown (`retriggerCooldownMs`) + delta thresholds to prevent visual spam/flicker while preserving visible sweep behavior.
- TASK3 (TIER4 feedback triggers in LinkConduit branch):
  - Added local 10 Hz threshold detector in `LinkRendererConduit.updateAll()` for link + node corruption crossings.
  - Wired `displayCorruptionSeed` on first rising threshold crossing.
  - Wired `displayCascadeWarning` on higher rising threshold crossing.
  - Wired `displayHarmonyPulse` on downward recovery crossing with cooldown guard.
  - Detector state persisted per link/node with hysteresis reset thresholds to keep triggers stable (no flicker spam).
- TASK4 (shared trail pool for spark/healing/corruption):
  - `LinkTrailParticleSystem` now has shared source API (`registerTrailSource`, `emitFromSource`) with per-type profiles and max-active budgets in one pool.
  - Added typed streams: `corruption`, `healing`, `spark` (per-type color/lifetime/emission/scale).
  - `LinkTrailEmitter` now routes through `emitFromSource` and supports explicit `sourceType`.
  - `LinkRendererConduit` maps existing systems into shared pool:
    - `LinkCorruptionParticleSystem` -> `type: 'corruption'`
    - `LinkHealingParticleSystem` -> `type: 'healing'`
    - `LinkSparkSystem` -> `type: 'spark'`
  - Added per-type rates + budgets to keep trails visible without overfilling the pool.
- Link runtime cost hotfix: TubeGeometry rebuild for conduit strands + skin is now cadence-gated in LinkRendererConduit.update() (geometryTick), so heavy geometry reallocation/dispose no longer runs every frame; first dynamic build still guaranteed.
- Geometry cadence tuned further: conduit dynamic strand/skin TubeGeometry rebuild now uses explicit 10 Hz gate (geometryTick run10 from updateAll) instead of 30 Hz heavy tick.
- Link create staging implemented in LinkRendererConduit: phased bootstrap (frame 0-9) now initializes subsystems progressively (skin -> strands -> pulse ring -> wave+dust -> streaks -> arcs -> beads -> bead trails -> sparks -> emitters/adapters).
- Performance pass: corruption particles switched from spawn-per-frame behavior to target-active-count behavior (quantized by corruption, immediate trim above target).
- LinkSemanticPictogramSystem_Enhanced caps reduced: per metric/state now 1, absolute per-link cap 5.
- Link pulseRing + arcDischarges updates cadence-gated to heavyTick (~30Hz) in LinkRendererConduit.
- Additional 30Hz gating in LinkRendererConduit: corruption spread animator, trail emitter update, dock/source injection helper VFX, and skin uniform updates now run on heavyTick cadence.
