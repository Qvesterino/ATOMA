# 2026-05-05

- Implemented raw gameplay metric split for the Network Time loop.
- `MetricsRuntime_v1` now maintains a raw canonical network snapshot separate from smoothed public live metrics:
  - preferred source: `NetworkMetricsAggregator.compute()`
  - fallback source: `_aggregateNodeMetrics()`
  - zero-link state resolves to raw zero metrics
- Added `MetricsRuntime_v1.getRawNetworkMetrics()` as the gameplay-safe accessor.
- `VisualNetworkTimeElasticity` in `main.js` now reads raw canonical `networkSynergy` from `metricsRuntime_v1.getRawNetworkMetrics()`, not `getCachedVisualMetrics()` / `__ATOMA_LIVE_METRICS__`.
- `MetricsRuntime_v1` now calls `_emitCanonicalSemanticMetrics()` from the raw snapshot path during update, instead of leaving that semantic tier path inert.
- `window.__ATOMA_LIVE_METRICS__` remains smoothed/public and still serves HUD/VFX consumers by default.
- HUD synergy was shifted behaviorally toward gameplay truth:
  - `CoreMetricsHUD` now prefers the score system's raw `avgSynergy` for the synergy display path
  - `_buildHudMetricsSnapshot()` in `main.js` also prefers raw gameplay synergy for AI/HUD-side reports
- Tests added:
  - raw snapshot prefers aggregator while public live metrics stay smoothed
  - raw snapshot falls back to node aggregation when aggregator is unusable
- Live Playwright smoke after the split confirmed:
  - natural smoke with `createSmokeLinks(6)` produced a clear divergence:
    - raw `networkSynergy ~0.347`
    - smoothed public `networkSynergy ~0.093`
    - score state `avgSynergy ~0.347`
  - this proves score now reads raw gameplay authority, not the lagged public/HUD smoothing layer
- Important remaining blocker:
  - the gameplay loop is still not naturally winnable in smoke
  - raw natural network synergy is still under threshold (`~0.35` with 6 smoke links in live runtime)
  - next likely phase is balance/aggregator sensitivity, not another score-authority refactor
- Headless Playwright still appears to under-represent real-time sustain pace because browser timing in background/headless mode throttles world time progression; use visible foreground browser for final real-time sustain/win verification.

- Implemented reachability balance pass focused on node-level synergy drag plus the canonical network aggregator.
- `src/metrics/NodeMetricEngine.js`
  - `SYNERGY_DERIVATION.corruptionDamping`: `0.85 -> 0.70`
  - `SYNERGY_DERIVATION.loadDamping`: `0.65 -> 0.50`
  - `deriveSynergyTarget()` blend now preserves more archetype identity: `0.72 archetype / 0.28 derived`
- `src/metrics/NetworkMetricsAggregator.js`
  - `compute()` is now the single hybrid quality-sensitive runtime path
  - removed unconditional audit console spam
  - removed random metrics fallback mutation for nodes missing canonical metrics
  - link quality now normalizes `normalizedScore`, `quality`, `score`, and `qualityScore` consistently into `0..1`
  - node contributions now use hybrid weighting (`0.6..1.2`) based on average connected link quality plus degree bonus
  - link influence now uses stronger release values and fairer mixed-category multiplier
  - diagnostics now expose `avgLinkQuality` and `aggregatorMode: 'hybrid'`
- `main.js`
  - added `window.__DEBUG.createReachabilitySmokeLinks(count = 6)` for deterministic QA smoke setup
  - helper now prefers good non-error nodes by derived reachability target, low corruption, and low load, instead of naive sequential linking
- Verification:
  - `node --check src/metrics/NodeMetricEngine.js`
  - `node --check src/metrics/NetworkMetricsAggregator.js`
  - `node --check main.js`
  - `node --check tests/GameplayLoopChecks.js`
  - `node tests/MetricsAuthority.test.js`
  - `node tests/GameplayLoopChecks.js`
- Live smoke on `http://localhost:5173/`:
  - deterministic reachability helper now reliably crosses canonical raw threshold in headless smoke (`raw networkSynergy ~0.45-0.49`)
  - public smoothed metrics still lag far behind raw, which is correct after the gameplay/public split
  - visible Playwright run confirmed `global.synergy.high` sustain reaches `REWIND` with score state showing `direction: REWIND`, `sustainProgress ~5.0`, and raw synergy around `0.48`
  - full `WON` end-to-end still was not locked in a later long smoke because that run's selected network drifted just under threshold; the reachability issue is materially improved, but true release reliability may still need one smaller second-pass sensitivity/balance tweak if human play reports it as too strict

- Implemented release-safe metrics authority cleanup:
  - `MetricAuthorityGuard` is now scope-aware (`node-canonical`, `global-canonical`, `link-canonical`, `visual-local`, `legacy-mirror`) instead of pretending every metric-shaped value is a canonical node writer
  - stale `ComputeSynergyScore2_1` node-writer registration removed; active scorer is reported as `ComputeSynergyScore2_0` link-local producer
  - `SafeMetricsDNAIntegration1_0` no longer installs a second proxy/authority guard; runtime canonical guarding stays in `NodeMetricEngine`
  - `CoreMetricsOverlay` now reads canonical runtime global metrics only; removed its node-average/event-fed authority path for Core HUD truth
  - `NodeInspectOverlay1_0` remains node-local by reading selected `node.userData.metrics` or that node's simulation snapshot
  - `PHASE5_MultiNetworkCore` now prefers canonical `node.userData.metrics.corruption/harmony` and link-local canonical corruption before legacy mirrors
- New QA console report:
  - `window.__ATOMA_METRIC_AUTHORITY_REPORT__()`
- Post-cleanup verification:
  - `window.__ATOMA_METRIC_AUTHORITY_REPORT__()` now reports only `NodeMetricEngine` under `canonicalNodeWriters` and only `MetricsRuntime_v1` under `canonicalGlobalWriters`
  - `HarmonyStabilization`, `HarmonicHealingVisualSystem_Session134`, `PHASE5_CorruptionBridge`, and `PHASE5_NetworkSynchronization` now appear as authorized node impulse sources instead of fake canonical writers
  - headless Playwright smoke remains good for authority/report validation, but not for final real-time sustain/win timing due to browser-time throttling and incomplete HUD selector reliability
