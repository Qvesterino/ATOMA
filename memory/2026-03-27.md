# 2026-03-27 Session Notes

## Cascade Particle Scene Attach Fix

- Added a self-healing scene attach guard to `CascadeParticleSystem_Session120.update()`.
- If `cascade.mesh.parent !== scene`, the mesh is re-added to `scene` on update.
- This protects the cascade render object from being detached by later cleanup or rebuild paths after init.

## Runtime Verification

- Verified in browser runtime after creating a test link.
- `CascadeParticleSystem_Session120.mesh` was attached to `Scene`.
- `mesh.visible = true`
- `mesh.parent = Scene`
- `attachedToScene = true`
- `activeCount = 100` after the test link.

## Notes

- `main.js` already had a setup-time reattach guard for cascade and particle trail meshes.
- The update-time guard in `CascadeParticleSystem_Session120` is the durable safety net.
