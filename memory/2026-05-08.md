## 2026-05-08

- Canonical live browser validation target is now `http://127.0.0.1:5173/` via Vite. `5500` is stale for primary runtime verification and should only be referenced as a legacy/static fallback.
- Spawn runtime hardening pass completed:
  - fixed runtime growth crash in `NodeSpatialIndex` by repairing malformed `_spatialSphere` data before insert
  - hardened `AINodes.registerInteractiveMesh()` so one spatial registration failure does not kill runtime growth
  - hardened `_processSpawnRequests()` so a failed queued growth request does not wedge the queue permanently
  - fixed missing `registryKey` / `unifiedUniqueKey` runtime growth variables in `AINodes`
  - made `nodeSpawnRegistry.registerSpawn(...)` optional-safe to avoid undefined global crashes
  - restored `EnhancedNodeModels.createControlNode(...)` legacy alias for runtime growth compatibility
  - moved `aiNodes.updateSpawning(...)` to simulation authority in `main.js`
- Spawn QA/debug surface now includes:
  - `window.__DEBUG.getActiveLinkSnapshot(limit?)`
  - `window.__DEBUG.removeLink(linkIdOrIndex?)`
  - `window.__DEBUG.pushLinkMetrics(linkIdOrIndex, metrics, options?)`
  - `window.__DEBUG.sustainLinkMetrics(linkIdOrIndex, metrics, { holdMs, pulseIntervalMs })`
- Link-threshold growth no longer hides behind `currentTier >= 1` in `NodeLinkingSystem`. Runtime growth now respects the intended `3 / 6 / 9 active links` milestone contract.
- Live runtime validation on `5173`:
  - `quantum` fresh boot: `15` init nodes, broad category spread, `duplicateVisualCount = 0`
  - `quantum` growth:
    - after `3` active links: `17` nodes, milestone `1`, queue drained
    - unlink from `3` to `2` then rebuild to `3`: no duplicate milestone re-fire
    - after `6` active links: `18` nodes, milestone `2`, queue drained
    - after `9` active links: `20` nodes, milestone `3`, queue drained
  - `desert` lighter smoke: fresh boot `15` nodes, broad category spread, `duplicateVisualCount = 0`; after `3` active links runtime grew to `16` nodes with milestone `1`
- Collapse QA path was corrected:
  - `LinkCollapseSystem.update()` remains intentionally inert
  - the reliable QA route is metric push through debug helper plus event-driven collapse evaluation and `runCollapseArbiter()`
  - `LinkCollapseSystem._readNormalizedMetrics()` / event precedence now honors incoming canonical metrics over stale cached link metrics
  - debug helper now writes canonical link metrics and explicitly forwards them into `LinkCollapseSystem.onLinkMetricsUpdated(...)` for deterministic QA
- Live collapse validation on `5173`:
  - release hold window remained noisy under normal runtime metric rewrites, so deterministic validation was performed with a temporary QA-only runtime override `holdDurationMs = 600`
  - under that QA hold override, sustained high corruption / low stability caused:
    - `totalCollapses: 0 -> 1`
    - active link count `1 -> 0`
    - target link removed through canonical collapse arbiter path
