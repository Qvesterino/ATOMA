# 2026-03-31

- Canonical debug/runtime link creation helper added:
  - `NodeLinkingSystem.createLinkById(sourceNodeId, targetNodeId)`
  - `window.__DEBUG.createLinkById(idA, idB)` as thin passthrough only
- The helper resolves nodes through the existing `NodeLinkingSystem` lookup and forwards to `createLink()` without duplicating link construction logic.
- `SynergyCascadeVisualizer` birth lifecycle is now driven by the canonical `link.created` event / link-birth helper path with the live link object, and its history retention is age-based instead of random cleanup.
- `WaveInterferencePatternSystem_Session132` now seeds birth interference from link creation, resolves link ids against the live linking authority, and preserves orphaned birth pairs through a grace lifecycle instead of deleting them immediately on unlink.
- Playwright smoke on 10 links confirmed `birthPairs = 10`, `zones = 165`, and `meshes = 15` immediately after creation; the runtime later cleared links and wave state at the 10s sample, which appears to be external world lifecycle traffic rather than a wave-system exception.
- A follow-up uninterrupted 30-second smoke kept `linkCount = 10` and `birthPairs = 10` the entire time, so the earlier 10-second zero-state was a dev-server live reload / session churn artifact during edits, not a deterministic runtime cleanup timer.
- Birth-seeded wave visuals now have a torus pulse ring, stronger spike bias, and a boosted emergence opacity so link birth reads as a distinct event instead of a generic flash.
- `StandingWaveVisualRenderer_Session131` trap zones now use a seed-like pulse ring to stay visually consistent with the newer wave birth-seed language.
