# 2026-03-28 Session Notes

## Link Unlink Cleanup

- `LinkedGlyphMessaging3_0.unregisterLink(linkId)` is now called directly from the main link removal wrapper and syncs its stats immediately.
- `SynergyCascadeVisualizer` now exposes `clearLink(...)` and hides pooled particles when they are returned, so unlinking no longer leaves stray particle meshes behind.
- `ResonanceCascadeVisualization_Session117B` now exposes `clearLink(...)` so unlink can drop active resonance waves tied to the removed link instead of waiting for decay.

## Runtime Verification

- Recreated a link in the browser runtime, then removed it.
- After unlink, `linkedGlyphMessaging.trackedLinks` and `activeMessages` were cleared.
- The visible orange resonance burst no longer remained at the unlink location in the screenshot after removal.

## Notes

- `SemanticGlyphAI` integrity mismatch console error is still present and predates this work.

## Phase 8 Ritual Reactivation

- `main.js` no longer ties the Phase 8 network ritual stack to the mythic ritual safety flag. Phase 8 now uses its own `disablePhase8NetworkRituals` flag, defaulting to enabled.
- `NetworkRituals_v1.js` was reconnected to the real `LinkCorruptionTransmission_v1` runtime state by reading `linkCorruption` / `linkIntegrity` maps and using `rebuildCollapsedLink(...)` for collapsed links.
- For non-collapsed links, Phase 8 ritual resolution now writes back into the active corruption/integrity runtime state instead of calling missing APIs (`getLinkState`, `rebuildLink`).
- `main.js::_setupRitualAutoTrigger()` now reads live corruption from `linkCorruptionTransmission.linkCorruption` before falling back to `link.userData.corruptionLevel`.
- `Phase8VisualBridge.js` now has a direct-render fallback path, so ritual visuals can modulate real node/link renderables even when `VisualAutoWiringSystem` has no wired controllers.
- `Phase8RitualVisualOrchestration.js` now reapplies modifier updates during the visual tick, so active ritual visuals continue evolving instead of staying frozen at first application.
- Added `window.PHASE8_RITUAL` debug API in `main.js` for manual Phase 8 ritual testing: `start`, `startAuto`, `cancel`, `status`, `list`, `stats`, `pickAuto`, `help`.
