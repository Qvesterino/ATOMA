## 2026-05-12

- Targeted render-cost containment pass for three active VFX systems:
  - `NodeLinkedAuraSystem`
    - removed per-node full-link scan from hot update path
    - now rebuilds one per-frame node→linkCount cache and reads counts from it
    - reduced `NodeSegmentedOrbitRings` budget in aura creation from `segmentCount: 64` to `48`
  - `WaveInterferencePatternSystem_Session132`
    - lowered demo/runtime budget:
      - `maxInterferenceMeshes: 18`
      - `maxConcurrentInterferences: 8`
      - `visualUpdateHz: 20`
      - `spikeCount: 3`
      - `lodDistance: 28`
    - reduced shared geometry detail (`SphereGeometry 10x10 -> 8x8`, pulse torus segments reduced)
    - stopped per-update array/filter churn for `interferenceMeshes`
    - cached average network-state modulation on a slower cadence instead of rescanning nodes every visual tick
  - `ResonanceCascadeVisualization_Session117B`
    - lowered shared geometry detail for cascade markers/rings
    - moved node/link influence sampling to `15Hz` while keeping visual decay/render state smooth on the normal visual cadence
    - replaced `activeCascades.includes(...)` cleanup scan with a `Set` membership pass
- Follow-up doc/runtime consistency repair:
  - `SafeQuantumIllusionsPack1.afterPaths.enabled` restored to `false`
  - `SafeQuantumIllusionsPack1.getStatus()` now exists and reports `afterPaths.enabled` + active count for debug truth
