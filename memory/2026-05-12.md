## 2026-05-12

- `process 209 / createExtremeProcess1` fully replaced — old FractalBloom (18 icosahedron meshes × 3 layers with unique materials via extremeNodePack) removed; now uses `PARANORMAL_FORGE` design:
  - Core: asymmetric TorusKnot (p=2, q=3) with emissiveIntensity 2.0 — the impossible self-intersecting process artifact
  - 3 asymmetric forge arms at unequal angles (0°, 130°, 250°) with different lengths (0.4, 0.32, 0.25) and tilts
  - 3 octahedron arm tip crystals (shared glow material)
  - Tilted rune ring torus (35° tilt) — ritual orbit
  - Wireframe icosahedron containment shell — the processing field boundary
  - Base vortex ring (tilted opposite to rune ring) — energy intake
  - Paranormal mist aura sphere — transparent, depthWrite false
  - Self-contained inline — no external pack dependency.
  - PERFORMANCE: 11 meshes, 5 materials (3 shared + 2 unique for core/shell). Learned from control node crisis.
- `integration 313 / createIntegrationNode1` — removed the black wireframe cage (`Wire chaos cage` section: perturbed BoxGeometry → EdgesGeometry → LineSegments). The node now shows only the two core blocks, bridge network, fragment plates, and orbit interference tube without the enclosing wireframe box.
