# 2026-03-19

- Link VFX visibility fix (LinkRendererConduit stack):
  - `LinkEnergyWave` was writing `emissiveIntensity` on strand `ShaderMaterial` (no visual effect). Wired to `uLocalLoad` uniform pulse path.
  - `LinkCorruptionSpreadAnimator` was writing `material.color/emissive` on strand `ShaderMaterial` (no visual effect). Wired to `uBaseColor` uniform path.
  - `WaveTravelShaderPack_v1` had custom-shader incompatibility and wrong uniform tracking key (`shader.material` missing). Added declaration injection + custom shader fallback replacements + material-keyed uniform tracking.
  - `LinkRendererConduit._attachWaveDirectionUniform` now runs previous `onBeforeCompile` first, then applies wave length/phase-offset rewrite.
