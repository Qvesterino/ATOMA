# 2026-03-21

- Stress visuals reactivation pass (safe wiring):
  - `CanonicalTemplate3_StressVisuals.updateNetworkStress()` now accepts both 0..1 and 0..100 stress inputs (normalizes to 0..1).
  - `StressVisualShaderSystem` now falls back to global stress providers (`window.ATOMA_NETWORK_STRESS()` / `window.NETWORK_STRESS`) when `coreMetricsCalculator` is absent.
  - `main.js` now explicitly enables stress ambient plane via `enableAmbientPlane()` and registers nodes to stress shader system in the visual scheduler pass.
  - Fixed shader init ordering in `shaders/StressVisualShaders.js` (`gl_Position` assigned before `vDepth` read).
- Key diagnostic lesson: stress visual systems were initialized but effectively inert due to missing stress source wiring + ambient plane not enabled.- main.js now initializes NetworkStressAggregator with correct constructor args and binds rameScheduler so update() is not short-circuited by missing scheduler.

- Stability VFX activation follow-up: HarmonicHubAuraSystem now receives { nodes, links } world view (instead of aiNodes only), enabling actual hub detection; ResonanceRuptureVisualSystem invalid duplicate cascade call (	riggerCascade) removed to prevent runtime break in rupture→cascade chain.

- Hub aura visibility fix: HarmonicHubAuraSystem_Session126 updated to support link endpoints in both formats (
odeA/nodeB and source/target) and harmony fallback (metrics.harmony, harmonyLevel, harmony), restoring hub qualification on current NodeLinkingSystem data model.

