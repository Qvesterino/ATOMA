# 2026-04-03

- Current runtime boot map for this workspace:
  - canonical static runtime: `http://127.0.0.1:5500/index.html`
  - Vite dev runtime: `http://localhost:5173/`
  - Python server: `http://localhost:8080/`
  - manual browser testing is being done in Microsoft Edge
- Core Metrics HUD smoothing was adjusted to be time-based again; current focus is NodeInspect overlay stability after first link creation.
- NodeInspect overlay work is now being validated against the static boot path rather than the Vite dev URL.
