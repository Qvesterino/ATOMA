- Implemented shared texture/material pooling in `LinkPointFXBase` for shareable point-cloud materials.
- Default sharing applies to shader-less point clouds; custom shader materials remain unique unless explicitly opted in.
- `LinkBeadSystem` should benefit immediately through `createPointCloud()`.
- `LinkCorruptionSpreadAnimator` now uses the shared point-sprite path for dust waves, with intensity moved to `points.scale` so the shared material stays stable.
- `LinkMicroImpulseAdapter_v1.js` is the active adapter; the older `LEGACY/LinkMicroImpulseAdapter.js` remains only as a legacy copy.
- `LinkMicroImpulseAdapter_v1.js` now applies micro-impulse color/opacity at draw time via `onBeforeRender` and uses `LINK_SPARKS` for the canonical render layer so link-creation cost stays lower.
- `LinkSparkSystem.js` now uses `VisualHierarchyRegistry` for the canonical `LINK_SPARKS` render order.
- `LinkResonanceFlowSystem_Session124.js` now applies `LinkRenderLayerPolicy` to the resonance pulse group and its parts so the material policy stays centralized with render order.
- `LinkRingArcDischarges.js` now renders arcs as a core line plus a lightweight glow line, with glow strength passed in as a small parameter on the arc spawn path.
- `LinkRingArcDischarges.js` glow was later intensified again by raising the default glow strength and opacity scaling while keeping the same core-plus-glow structure.
- `LinkRingArcDischarges.js` line width values are now treated as best-effort hints; the visible gain comes primarily from glow and opacity shaping.
- `LinkPulseRing.js` and `LinkRingArcDischarges.js` now expose rebindable attach roots so their visual roots can be reattached cleanly on world switch.
- `LinkTrailParticleSystem.js` is back at its baseline particle sizing and opacity after the reverted visibility experiment.
- `LinkTrailParticleSystem` now exposes a rebindable pool root so world-switch reattachment can happen through `rebind({ scene, worldRoot })` instead of relying on a constructor-only scene add.
- `LinkHealingParticleSystem.js` now mixes the original healing knot sprite with a new `Bloom Petal` variant on a 50/50 basis, still inside one `THREE.Points` pool.
- `LinkRendererConduit.js` strand filaments now use a slower, heavier motion profile with a modest flow wave and a `linewidth` hint in place; the visible gain comes from motion shaping, not line width alone.
- `main.js` runtime node lookup helpers now prefer `aiNodes.nodes` / canonical registries over `scene.traverse(...)` when resolving nodes by id, code, or name.
- The resonance-only debug visibility toggle now caches hide targets after its first scene scan, so repeated toggles avoid another full scene traversal.
- `LinkRendererConduit` braid geometry rebuilds are now bootstrap-only; after the initial build they no longer rebuild on node motion or radius drift.
- `LinkRendererConduit` skin geometry rebuilds are also bootstrap-only; the runtime no longer recompiles the skin `TubeGeometry` after the initial build.
- `LinkRendererConduit` strand bootstrap now creates one strand per bootstrap tick instead of allocating all strand meshes and materials in a single frame, and locked Frenet frames are cached during bootstrap.
- `LinkRenderLayerPolicy` now exposes the canonical bootstrap budget helper for link creation cadence, and `LinkRendererConduit.updateAll()` uses it to time-slice bootstrap advancement across frames.
- `LinkRenderLayerPolicy` now also owns the per-link effect cadence policy, and `LinkRendererConduit` uses a frame-indexed scheduler so corruption/trail/healing updates can phase-shift instead of all landing in the same frame.
- `beads`, `bead trails`, `pulseRing`, `arcDischarges`, and `ringPulseDustEmitter` are now cadence-gated through `LinkRenderLayerPolicy` instead of being forced through the same heavy tick branch.
- Particle families now use `particleSystem` cadence 2 in `LinkRenderLayerPolicy`, covering sparks, trail/healing particles, corruption particles, and pulse dust updates.
- Current cadence map: `resonanceFlow` 1, `pulseRing`/`arcDischarges`/`ringPulseDustEmitter` 1, `beads`/`beadTrails` 1, `energyRingSystem`/`directionalStreaks` 2, trail/healing/corruption particle emitters and particle systems 2, `corruptionSpread` 1.
- `impactManager` stays event-driven through particle arrival callbacks instead of being cadence-driven.
- `LinkCorruptionMorphingSystem` moved to `LEGACY/LinkCorruptionMorphingSystem.js`, and `LinkRendererConduit` no longer instantiates or updates it in the active runtime.
- The active cadence policy no longer carries `corruptionMorph`; it is legacy-only now.
- `LinkMicroImpulseAdapter_v1.js` now keeps a dedicated impulse root group that is reattached on world switch via `rebind({ scene, worldRoot })`, so impulses survive world-root rebuilds more cleanly.
- Runtime smoke confirmed the micro impulse path only starts spawning once it resolves the link's `TubeGeometry` from `link.group.children[0]`; `link.geometry` is not populated on the active link objects.

- GlyphFusionZone and LinkSemanticPictogramSystem_WithFusion heartbeat/lifecycle logs are now opt-in debug only, so the normal runtime console is quieter and the log spam no longer lands as console.error.

- The Network Fatigue console API bootstrap message now uses console.info instead of console.error, so successful init is no longer reported as an error in the normal runtime console.
- `EnvironmentDomainController` is now the single active owner of `EnvironmentalHazards` in the normal boot path; `main.js` binds `this.hazards` to that instance after init to avoid duplicate hazard updates from a standalone boot instance.
- `LinkSemanticPictogramSystem_Enhanced` opacity fades no longer set `material.needsUpdate = true`, so the runtime stays on the existing shader program path instead of forcing avoidable renderer churn.
- `NeonEdgeGlowShader` is now frozen in the active node path: `AINodes` no longer advances its `time` uniform, so the edge glow stays static instead of pulsing every frame.
- `TIER4_CorruptionFeedbackVisuals_v1.js` corruption seed visuals are now static after spawn: the shader no longer uses `uTime`, and the update loop no longer drives fragment orbit/pulse rotation or connection pulsing.
- `LinkRendererConduit` bootstrap now time-slices the heaviest create stages instead of completing them in one frame: stage 1 creates one strand per bootstrap tick, stage 2 defers pulse-ring trails to a second slice, and stage 4 defers directional streak pulse tracking to a follow-up slice.
- `LinkPulseRing` is a live animated root and must not be frozen with `matrixAutoUpdate = false`; its core segmented ring needs runtime transforms to stay visible.
