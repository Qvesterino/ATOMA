# Session 2026-04-13 — Visual Polish Continuation

## MetricReactiveWorldEvents Epic Visual Upgrade
- All 25 effect material creation sites upgraded from flat MeshBasicMaterial to epic glow materials
- Added `_createGlowMaterial(color, opacity, extra)` helper: AdditiveBlending + toneMapped: false + depthWrite: false + DoubleSide
- Added `_createGlowLine(color, opacity, extra)` helper: same for LineBasicMaterial
- Opacity increases across the board: rings 0.18→0.35, particles 0.3-0.6→0.55-0.8, beams 0.18→0.4, glyphs 0.3→0.8-0.9
- Removed invalid `emissive`/`emissiveIntensity` properties (MeshBasicMaterial silently ignores these)
- Effects affected: shimmer overlay, color tint, concentric rings, soft halo, thin arc bands, short beam spikes, fractured ring core, glitch bars, pressure bands, orbiting motes, orbital particles, floating particles, sky glyph, spiral glyph, rotating particles, radial gradient, void fragments, magenta line, expanding ring, arc sweep, golden glyph, sky beam, halo effect, distortion overlay, horizon pulse
