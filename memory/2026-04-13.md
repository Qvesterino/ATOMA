# Session 2026-04-13

## Lore Codex Reading Order
- Added explicit `readingOrder` notes to the `Codex` landing entries in `LoreRegistry.js`.
- Updated `MainMenu.js` and `PauseMenu.js` so the Codex featured card and archive cards surface those notes directly in the menu.
- Added a suggested reading order section to `docs/lore/canon/README.md` so the menu and canon index stay aligned.

## Source Lore Scope Check
- Confirmed there are no additional hidden backbone lore chapter files in `docs/lore`.
- Remaining Slovak lore material is the existing source/reference set plus the outline and canon index documents.

## AtomaLanguageEngine3_0 Upgrade
- `AtomaLanguageEngine3_0` is now a themed oracle strip instead of a plain cyan text box.
- Lore, node, link, pulse, and metric messages each get their own tone and palette.
- The engine now uses a serif body font with a cleaner sans-serif tag font, so lore contact reads as a deliberate in-game artifact.

## Canon Chapter Lore Messages
- Added specific lore trigger lines for the completed canon chapters in the Codex.
- `lore.unlocked` now maps `codex-*` entries to chapter-specific oracle lines instead of generic placeholders.
- Added per-chapter subtexts such as `Chapter unlocked` and `Canon revealed`.
- Added a stronger `lore.unlocked` reveal burst so chapter unlocks feel like an epic moment instead of a flat toast.

## Sacred Non-Codex Unlocks
- Non-codex lore unlocks now also use reveal bursts instead of plain text.
- Their subtexts are styled more like sacred annotations: shorter, more ritual, less HUD-like.
