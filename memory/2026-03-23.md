- ResonanceRuptureVisualSystem_Session133 switched to hybrid event-driven rupture triggering (existing semantic tags: link:collapsed, cascade.hop, metric.corruption.spike, metric:corruptionRise, metric:stabilityDrop, metric:loadPressureHigh, network:stressRise). Added per-link eventPressure decay, global stress bias decay, and merged totalPressure formula with existing trap stress/amplitude; no gameplay or visual style changes, only trigger-source wiring.
- Implemented Critical 5 canonical writes for links: CascadeEventBridge_v1 now writes cascadeIntensity/cascadeConflictType/conflictIntensity every visual frame for every active link; ParticleSemanticDensityAdapter_Session121 now writes particleIntensity/particleUrgency every update for all links (removed low-value skip), with cascadeIntensity+flowState fallback sources for intensity.
- Added canonical fallback initialization per-link during runtime updates (neutral defaults) in ResonanceRuptureVisualSystem_Session133 and CascadeParticleSystem_Session120; wired rupture pressure to canonical cascadeIntensity/conflictIntensity and particle spawning to canonical cascadeIntensity/cascadeConflictType to eliminate undefined-gated silent no-op paths.
- Implemented Critical 2nd wave canonical writes: StandingWaveOscillationTrapSystem now resets/writes node.userData.resonance and node.userData.waveField.amplitude/phase every update; ResonanceRuptureVisualSystem now guarantees per-link visualTear/visualCoherenceLoss defaults and writes rupture/scar-derived values every frame with decay baseline.
- MetricsRuntime_v1 now applies per-step canonical node fallbacks for harmonyStabilized/harmonyDampingFactor/loadPressure/pressure/instability and stamps write timestamps; added periodic canonical field audit (5s) warning on missing or stale writes for these fields.
