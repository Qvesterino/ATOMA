- Harmonic hub chain activation finalized: assigned frameScheduler to HarmonicHubAuraSystem, normalized node key usage (
odeId/id/uuid), refreshed existing hub membership each update, and late-wired linkResonanceSystem alias to LinkResonanceFlowSystem_Session124 for HubAura/Cascade/PreCascade consumers expecting globalPulses API.


- Corruption VFX reactivation wiring pass: CascadeEventBridge now subscribes to both metric:corruptionRise and canonical metric.corruption.spike; main.js now ticks corruptionDesaturation.update() on visual lane each frame and lowered nodeCorruptionFX gate from corruption > 0.75 to > 0 so CorruptionVisualFX can express non-zero corruption continuously.
- Runtime smoke pass (Playwright): confirmed corruption stack instantiated and scheduler-registered (isual.corruptionDesaturation, isual.corruptionAuraDesaturation, isual.nodeCorruptionFX, isual.cascadeParticleSystem, isual.cascadeEventBridge), confirmed CascadeEventBridge bound handler is subscribed to both metric:corruptionRise and metric.corruption.spike, confirmed CorruptionVisualFX executes for low corruption (0.1). Runtime world currently had 0 links, so full event→link-flow visual chain remains blocked until at least one real link exists.
- Runtime re-smoke on Fractal Valley + Quantum Island: corruptionVisualFX/desaturation ticks are active and low-corruption gate works, but after world switch CascadeEventBridge is not scheduler-registered (isual.cascadeEventBridge=false) and bridge handler is not bound on corruption tags (metric:corruptionRise, metric.corruption.spike) despite bridge object existing. Both worlds also showed linkCount=0 in this automation session, blocking full link-chain visual validation.
- CascadeEventBridge world-switch fix applied: main.js now nulls bridge on disposal, rebinds via ensureCascadeEventBridge() after createAINodes in createWorld, and setupCascadeParticleSystem uses same idempotent bridge rebinder. Playwright re-smoke on Fractal->Quantum confirmed bridge stays scheduler-registered (isual.cascadeEventBridge=true) and remains subscribed to both corruption tags across switches.
- World-switch lifecycle audit (cascade/wave/resonance/particle): scheduler registrations for 38 relevant systems remained stable across Fractal<->Quantum switches, but semantic handler counts for metric.corruption.spike, cascade.hop, cascade.start, cascade.end increased each switch, indicating duplicate listener accumulation likely from systems reinitialized without explicit dispose/unsubscribe (observed handler signatures: repeated handleCorruptionSpike, enderCascadeStart/End/Hop, spawnCascadePropagationVisual).
