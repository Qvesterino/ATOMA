## 2026-05-07

- Implemented public map lock policy for release gating:
  - public worlds: `quantum`, `desert`
  - coming-soon locked worlds: `memory`, `sigma`, `desert2`, `fractal`
- Added persistent local dev unlock via `localStorage['atoma.dev.unlockMaps']`.
- Hardened menu, pause menu, and boot entrypoints so locked worlds fall back to `quantum` when dev unlock is off.
- `window.atomaApp` now exposes `setMapDevUnlock(enabled)` and `isMapDevUnlockEnabled()`.
- Added Default HUD Layout V1 bootstrap for first-launch positioning:
  - left column = category legend, node inspector, core metrics
  - right debug stack keeps wave anchor and aligns AI automation under it
  - saved HUD drag positions remain authoritative and are not migrated
- Fixed AI Automation right-stack drift:
  - automation HUD now reapplies canonical wave-anchor positioning after remount/visibility sync
  - selected, wave, and advisor HUDs remain untouched
- Fixed developer HUD visibility contract drift:
  - `ui/config/UIVisibilityConfig.js` now exposes effective developer HUD visibility helpers instead of only raw saved preferences
  - settings rows for `AI Automation` and `Advisor HUD` now show effective runtime truth, not just raw toggle state
  - `HUDLayerManager` now rebroadcasts the shared UI visibility event when F4 developer mode changes
  - `AIAutomationHUD` and `VariantBAdvisorHUD` now respect effective visibility on mount, remount, and update paths
  - `main.js` AI/advisor report refresh now keys off effective visibility, removing hidden/zombie update states when developer mode is off
- Reframed optional HUD gating after product clarification:
  - `AI Automation` and `Advisor HUD` are now menu-controlled optional overlays, not F4-gated developer-only panels
  - both default to `OFF` on fresh visibility config, but toggle directly to visible from menu
  - `Wave System HUD` remains the only developer-gated panel in that settings cluster
  - HUD registry / layer manager now use per-HUD `requiresDevMode` policy instead of assuming every `DEBUG_AUTHORING` panel is hidden behind F4
- Added targeted VFX runtime status audit for:
  - `TopologyBiasVisualizationLayer`
  - `ProceduralHarmonicGlyphGenerator`
  - `SynapticFatigueAdapter_v1`
  - `SynapticGatingAdapter_v1`
  - `SynapticSpecializationAdapter_v1`
  - `SynergyChainReaction_v1`
- Audit conclusion:
  - strongest live/visible status: topology bias layer
  - live but subtle: procedural glyph generator, synaptic fatigue
  - active but latent: synaptic gating
  - most suspicious wiring candidates: synaptic specialization and synergy chain reaction
- Ran mixed runtime verification pass for `SynapticSpecializationAdapter_v1` and `SynergyChainReaction_v1`:
  - canonical live runtime target was the in-app browser on `http://127.0.0.1:5500/index.html`
  - in-app browser confirmed actual gameplay boot and node spawn activity, but page-context JS introspection was limited from that surface
  - external Playwright boot was inconsistent and slow because `index.html` starts with `window.__ATOMA_SKIP_AUTO_BOOT = true`; menu/game boot in that separate profile was not deterministic enough for a clean forced status capture
  - practical conclusion remains:
    - `SynapticSpecializationAdapter_v1` is live-wired on simulation tick and should be treated as active but not visibly legible in current gameplay
    - `CompetitionDominanceAdapter_v1` still looks like the missing downstream payoff path for specialization
    - `SynergyChainReaction_v1` is registered and live-wired, but current runtime evidence still does not show a visible or observable consumer payoff
    - current live gameplay sample also had `Wave System` showing `Active Links: 0`, so automatic chain reaction triggering was not expected in that state
- Implemented `CompetitionDominanceAdapter_v1` activation + readable specialization payoff:
  - added runtime scheduler wiring in `main.js` on `simulation.competitionDominance`
  - fixed dominance reads to canonical `node.userData.metrics` and `node.userData.synapticBias`
  - no-link / solo topology now resolves to neutral instead of making every node look dominant
  - dominance now writes a readable modulation payload under `node.userData.visualState.dominance.modulation`
  - `HarmonyStabilization` now applies that modulation into live `haloAmplitude`, `haloFrequency`, `pulseCoherence`, `pulseStreak`, and `pulsePhase`
  - `competitionDominance.getStatus()` now includes update count, role counts, and a small node sample for quick runtime truth
- Implemented demo latent-systems containment pass:
  - added central release policy authority in `src/runtime/AtomaReleaseContainmentPolicy.js`
  - demo defaults now set `ATOMA_DISABLE_SYNERGY_CHAIN_REACTION = true`
  - `main.js` now respects release containment policy during chain-reaction init and scheduler wiring
  - `SynergyChainReaction_v1` is skipped entirely in demo profile: not initialized, not scheduled
  - `SynapticGatingAdapter_v1` remains active but is formally classified as support-only
  - exposed runtime truth helpers:
    - `window.__ATOMA_RELEASE_CONTAINMENT_PROFILE__()`
    - `window.__ATOMA_RELEASE_CONTAINMENT_STATUS__()`
  - added release audit doc:
    - `docs/audits/DEMO_VFX_RELEASE_CONTAINMENT_2026-05-07.md`
  - light in-app browser reload smoke on `http://127.0.0.1:5500/index.html` still reached the main menu cleanly after the containment wiring
- Implemented spawn-system hardening + freshness pass:
  - `SessionVariantEngine.js` now owns run-scoped per-category shuffled bags instead of simple counter cycling
  - `AINodes.js` now builds the init spawn batch with broad category spread:
    - first pass gives every valid category one slot before duplicates
    - remainder uses least-represented-first with run-scoped shuffled tiebreaks
  - initial spawn no longer force-injects `mythic + prime` as a tail hack
  - runtime growth selection is now unified with init selection:
    - category choice uses least-represented-first
    - visual choice uses the same run-scoped category bag authority
    - uniqueness is preserved while unused visual codes still exist in the chosen category
  - fixed init/runtime spawn-cap contradiction:
    - `createNodes()` no longer leaves runtime capped to the init batch size
    - `armRuntimeSpawningAfterInit()` now clears the init-only hard cap and restores runtime growth targets
    - `main.js` now calls `aiNodes.armRuntimeSpawningAfterInit(Date.now())` after the init batch instead of ad-hoc flag pokes
  - added runtime spawn QA helpers:
    - `window.__ATOMA_INIT_SPAWN_SUMMARY__()`
    - `window.__ATOMA_SPAWN_STATUS__()`
    - `window.__ATOMA_SPAWN_DISTRIBUTION__()`
  - test coverage added in `tests/NodeGrowthSpawnChecks.js` for:
    - 3-link / 6-link milestone growth
    - broad-spread init plan
    - different run seeds producing different orders
    - least-represented runtime growth selection
    - runtime arming clearing the old init-only cap
