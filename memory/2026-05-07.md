## 2026-05-07

- Implemented public map lock policy for release gating:
  - public worlds: `quantum`, `desert`
  - coming-soon locked worlds: `memory`, `sigma`, `desert2`, `fractal`
- Added persistent local dev unlock via `localStorage['atoma.dev.unlockMaps']`.
- Hardened menu, pause menu, and boot entrypoints so locked worlds fall back to `quantum` when dev unlock is off.
- `window.atomaApp` now exposes `setMapDevUnlock(enabled)` and `isMapDevUnlockEnabled()`.
- Added Default HUD Layout V1 bootstrap for first-launch positioning:
  - left column = category legend, node inspector, core metrics
  - right debug stack keeps wave anchor and aligns AI automation under it
  - saved HUD drag positions remain authoritative and are not migrated
- Fixed AI Automation right-stack drift:
  - automation HUD now reapplies canonical wave-anchor positioning after remount/visibility sync
  - selected, wave, and advisor HUDs remain untouched
- Fixed developer HUD visibility contract drift:
  - `ui/config/UIVisibilityConfig.js` now exposes effective developer HUD visibility helpers instead of only raw saved preferences
  - settings rows for `AI Automation` and `Advisor HUD` now show effective runtime truth, not just raw toggle state
  - `HUDLayerManager` now rebroadcasts the shared UI visibility event when F4 developer mode changes
  - `AIAutomationHUD` and `VariantBAdvisorHUD` now respect effective visibility on mount, remount, and update paths
  - `main.js` AI/advisor report refresh now keys off effective visibility, removing hidden/zombie update states when developer mode is off
- Reframed optional HUD gating after product clarification:
  - `AI Automation` and `Advisor HUD` are now menu-controlled optional overlays, not F4-gated developer-only panels
  - both default to `OFF` on fresh visibility config, but toggle directly to visible from menu
  - `Wave System HUD` remains the only developer-gated panel in that settings cluster
  - HUD registry / layer manager now use per-HUD `requiresDevMode` policy instead of assuming every `DEBUG_AUTHORING` panel is hidden behind F4
