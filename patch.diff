*** Begin Patch
*** Update File: AINodes.js
@@
     console.log('[SPAWN TRACE] created node object', {
       nodeId: newNode.userData.id || newNode.uuid,
       category: category,
       modelName: newNode.userData.nodeGeometryName || 'unknown',
       position: (, , )
     });
+
+    // Runtime integrity check: ensure freshly spawned node is attached to scene
+    this._checkDetachedNodes();
 
     return newNode;
     } catch (err) {
       console.warn('[SPAWN] aborted due to exception', err);
       return null;
@@
     }
     profileEnd('perNodeLoop', perNodeStart);
+
+    // Runtime integrity check: detect nodes that lost their scene parent
+    this._checkDetachedNodes();
 
     // AURA LOD CULLING: Update aura visibility based on distance
     const auraStart = profileStart('auraLOD');
     if (this.auraLOD && this.camera) {
       this.auraLOD.updateCulling(this.nodes, this.camera, deltaTime);
@@
     this.spawnVisualStats = (typeof window !== 'undefined'
       ? (window.__spawnVisualStats = window.__spawnVisualStats || { queued: 0, executed: 0 })
       : { queued: 0, executed: 0 });
     this._inputBypassLogged = this._inputBypassLogged || false;
     
     // [SESSION 110] Node Spawn Registry - Single-Instance Enforcement
     // Tracks active nodes to prevent duplicates of unique archetypes
     this.nodeRegistry = new Map();
   }
+
+  /**
+   * Integrity guard: log any nodes that are no longer parented to the scene
+   */
+  _checkDetachedNodes() {
+    for (const node of this.nodes) {
+      if (!node) continue;
+      if (node.parent !== this.scene) {
+        console.error('[NODE DETACHED]', node.userData?.nodeId || node.uuid, {
+          parent: node.parent?.type or null
+        });
+      }
+    }
+  }
*** End Patch
